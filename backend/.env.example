# Copy file này thành .env (cùng thư mục) trước khi chạy `docker compose up`.
# .env đã bị gitignore ở gốc repo — không commit file .env thật.
DB_USER=postgres
DB_PASSWORD=doi-mat-khau-nay
DB_NAME=SEAL-Database
